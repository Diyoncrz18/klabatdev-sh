import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "KlabatDev - Software House Terpercaya | Website, Aplikasi & Design",
  description: "KlabatDev adalah software house profesional yang menyediakan jasa pembuatan website, aplikasi mobile, design UI/UX, dan solusi digital lainnya. Lihat portfolio proyek kami dan hubungi kami untuk kebutuhan digital Anda.",
  keywords: "software house, pembuatan website, aplikasi mobile, design UI/UX, jasa website, jasa aplikasi, klabatdev",
  icons: {
    icon: [
      { url: '/logo.png', sizes: '32x32', type: 'image/png' },
      { url: '/logo.png', sizes: '16x16', type: 'image/png' },
    ],
    shortcut: '/logo.png',
    apple: [
      { url: '/logo.png', sizes: '180x180', type: 'image/png' },
    ],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body
        className={`${inter.variable} ${poppins.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
